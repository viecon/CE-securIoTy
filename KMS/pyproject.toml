[project]
name = "kms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask-cors>=5.0.1",
    "flask>=3.1.1",
    "pycryptodome>=3.22.0",
]
